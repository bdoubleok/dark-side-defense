# Target Figma file

Place the full **Inmate Profile** wireframe under this node:

| | |
|---|---|
| **File** | [Unified Command — Activities](https://www.figma.com/design/DzbyDszCqh5R0FzdL1etra/Unified-Command---Activities?node-id=831-1067&m=dev) |
| **File key** | `DzbyDszCqh5R0FzdL1etra` |
| **Anchor node** | `831:1067` (URL: `831-1067`) |

## Steps

1. Open the file in **Figma Desktop** (required for plugin + node lookup).
2. Select or navigate to node **831-1067** (Dev Mode link above).
3. Run plugin: **Inmate Profile Wireframe Generator** (see [README.md](README.md)).
4. Plugin creates frame **`Inmate Profile — Wireframe`** below or inside the anchor, containing:
   - Shell preview
   - Component library strip
   - All 12 section frames (incl. visitation + monitoring variants)
   - Annotation notes

If the node ID is missing (wrong file), the plugin falls back to creating pages `00 —` through `03 —` and shows a notification.

## Cursor / Figma MCP

When Figma MCP is connected, the same anchor can be used with:

- `fileKey`: `DzbyDszCqh5R0FzdL1etra`
- `nodeId`: `831-1067`
